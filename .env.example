# Example environment variables for local and Docker deployments

# Database
DB_HOST=database
DB_PORT=5432
DB_NAME=saas_db
DB_USER=postgres
DB_PASSWORD=postgres123

# JWT
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long-for-security
JWT_EXPIRES_IN=24h

# Server
PORT=5000
NODE_ENV=production

# Frontend URL (for CORS)
# Use http://frontend:3000 when running with Docker Compose
# Use http://localhost:3000 for local development
FRONTEND_URL=http://frontend:3000
